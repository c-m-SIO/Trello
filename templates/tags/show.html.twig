{% extends 'base.html.twig' %}

{% block title %}Tags{% endblock %}

{% block body %}
    <h1>Tags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ tag.libelle }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ tag.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tags_index') }}">back to list</a>

    <a href="{{ path('app_tags_edit', {'id': tag.id}) }}">edit</a>

    {{ include('tags/_delete_form.html.twig') }}
{% endblock %}
