{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.titre) }}
    </div>
    <div class="form-group" style="margin-top: 20px;">
        {{ form_row(form.description) }}
    </div>
    <div class="form-group"style="margin-top: 20px;">
        {{ form_row(form.date_fin_prevu) }}
    </div>
    <div class="form-group"style="margin-top: 20px;">
        {{ form_row(form.submit) }}
    </div>
{{ form_end(form) }}
