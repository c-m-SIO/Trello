<form method="post" action="{{ path('app_colonnes_delete', {'id': colonne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colonne.id) }}">
    <button class="btn">Delete</button>
</form>
